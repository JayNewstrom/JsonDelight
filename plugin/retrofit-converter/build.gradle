apply plugin: 'kotlin'

sourceCompatibility = 1.7

dependencies {
    compile project(':runtime')
    compile dep.retrofit
    compile dep.kotlinStdLibJdk7
}

apply from: rootProject.file('publish.gradle')
