buildscript {
    repositories {
        jcenter()
        mavenLocal()
    }
    dependencies {
        classpath dep.androidPlugin
        classpath "com.jaynewstrom.json:gradle-plugin:$ARTIFACT_VERSION"
    }
}

repositories {
    mavenLocal()
}

apply plugin: 'com.android.library'
apply plugin: 'com.jaynewstrom.json'

android {
    compileSdkVersion 25
    buildToolsVersion '26.0.0'

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 25
    }
}

dependencies {
    annotationProcessor 'com.google.auto.value:auto-value:1.4.1'
    provided 'com.jakewharton.auto.value:auto-value-annotations:1.4'
    compile "com.jaynewstrom.json:retrofit-converter:$ARTIFACT_VERSION"

    testCompile dep.junit
    testCompile dep.fest
    testCompile 'com.squareup.okhttp3:mockwebserver:3.8.1'
}

json {
    createSerializerByDefault true
    createDeserializerByDefault true
}
